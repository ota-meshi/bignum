---
"@bignum/vite-plugin": minor
---

feat: add a dedicated Vite plugin for pre-compiling BigNum template literals

Add `@bignum/vite-plugin` so Vite projects can enable the optimization with `plugins: [bignum()]`, with support tested against Vite 5, 6, 7, and 8.
